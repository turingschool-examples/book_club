<h1>All Books</h1>

	<% @books.each do |book| %>
		<%= book.title %>
		<%= book.publication_year %>
		<%= book.pages %>
		<% book.author.each do |auth| %>
			<%= link_to "#{auth.name}", "/authors/#{auth.id}" %>
			<% end %>
	<% end %>
